You're off the waitlist! — {{ ride.title }}

A spot opened up and you're now confirmed for {{ ride.title }} on {{ ride.date.strftime('%A, %B %d, %Y') }} at {{ ride.time.strftime('%I:%M %p') }}.

Meeting point: {{ ride.meeting_location }}
Distance: {{ ride.distance_miles }} miles

View ride: https://paceline.club/clubs/{{ ride.club.slug }}/rides/{{ ride.id }}
